searchd
{
        query_log_format = sphinxql
        query_log = query.log
        listen = 9312
        listen = localhost:9308:http
        listen = 9306:mysql
        listen = 9309:mysql_vip
        log = searchd.log
        pid_file = searchd.pid
        binlog_path = #
        data_dir = data
        
        threads = 4
}
